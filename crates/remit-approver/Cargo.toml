[package]
name = "remit-approver"
description = "Remit approvers (SPEC section 10): a model or rules may approve one action at a time, only inside a bound a human signed, failing towards a person. Pure, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
remit-core = { path = "../remit-core" }
serde_json = "1.0.151"
sha2 = "0.11.0"

[dev-dependencies]
proptest = "1.11.0"
