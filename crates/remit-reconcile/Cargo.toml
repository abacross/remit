[package]
name = "remit-reconcile"
description = "The Remit reconciler: joins the cloud provider's own record to warrants, in both directions, and states what it could and could not establish (SPEC section 6). Pure, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
remit-core = { path = "../remit-core" }
remit-aws = { path = "../remit-aws" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
