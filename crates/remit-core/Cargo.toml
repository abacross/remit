[package]
name = "remit-core"
description = "Warrants for AI agents: the authorization decision, attenuation, and canonical identity (SPEC sections 3 and 4). Pure, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
sha2 = "0.11.0"

[dev-dependencies]
proptest = "1.11.0"
