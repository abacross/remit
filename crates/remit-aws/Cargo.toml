[package]
name = "remit-aws"
description = "Remit on AWS: compiles a warrant to an STS session policy that never allows more than the warrant (SPEC section 8). Pure, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
remit-core = { path = "../remit-core" }

[dev-dependencies]
proptest = "1.11.0"
serde_json = "1.0.151"
